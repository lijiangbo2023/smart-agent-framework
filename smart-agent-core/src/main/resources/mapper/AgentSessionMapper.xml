<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.agent.persistence.mapper.AgentSessionMapper">

  <insert id="upsertSession">
    INSERT INTO agent_session
        (user_id, agent_name, session_id, data_key, data_value)
    VALUES
        (#{userId}, #{agentName}, #{sessionId}, #{dataKey}, #{dataValue})
    ON DUPLICATE KEY UPDATE
        data_value = VALUES(data_value),
        gmt_modified = CURRENT_TIMESTAMP
  </insert>

  <delete id="deleteExpiredRecords">
    DELETE FROM agent_session WHERE gmt_modified &lt; #{cutoffDate}
  </delete>

</mapper>
